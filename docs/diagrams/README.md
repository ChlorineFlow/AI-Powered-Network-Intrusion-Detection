# Diagrams

Place architecture diagrams, ER diagrams, or exported visuals here
(e.g. PNG/SVG exports of the system architecture, ML pipeline flow, or
database schema described in `docs/architecture/`). No diagram image
files are currently committed — the architecture is described in text
form in `docs/architecture/system-architecture.md` and `ml-pipeline.md`.